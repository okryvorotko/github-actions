name: Caller1
on:
  workflow_dispatch:

jobs:
  preparations:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Create file
        id: create-file
        run: |
          echo "file content" > filename
          FILE_CONTENT=$(cat filename)
          echo "GITHUB_VAR=$FILE_CONTENT" >> $GITHUB_OUTPUT
    outputs:
      GITHUB_VAR: ${{ steps.create-file.outputs.GITHUB_VAR }}


  run_reusable:
    needs: preparations
    uses: ./.github/workflows/reusable.yml
    with:
      command: npm run reusable1
      someInput: Input from Caller1
      FILE: ${{needs.preparations.outputs.GITHUB_VAR}}

  post:
    needs: run_reusable
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Delete file
        run: rm -rf filename
